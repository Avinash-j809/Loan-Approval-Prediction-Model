# Loan-Approval-Prediction-Model
Built a machine learning model to predict loan approval using applicant financial and demographic data. Performed data cleaning, EDA, and feature encoding, then trained Logistic Regression, Decision Tree, and Random Forest models. Random Forest performed best, with credit history as the key predictor. Built using Python and Scikit learn.
